/*
* @dev channel type
* */
export enum KodMobiChannel {
  WHATS_APP = 'whatsapp',
  TELEGRAM = 'telegram',
  SMS = 'sms',
}

/*
* @dev supported language (only response messages)
* */
export enum KodMobiLang {
  EN = 'en',
  RU = 'ru'
}
